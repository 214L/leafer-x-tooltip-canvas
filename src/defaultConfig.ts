import { IUserConfig } from './interface'
export const defaultConfig: IUserConfig = {
  reference: 'pointer',
  effect: 'light',
  showDelay: 0,
  arrow: false,
  hideDelay: 0,
  placement: 'right-end',
  offset: [0, 0],
  preventOverflow: false,
  includesType: [],
  excludesType: [],
}
